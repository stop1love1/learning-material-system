// Replays the page-enter animation when entering an immersive practice screen.
export default function FocusTemplate({ children }: { children: React.ReactNode }) {
  return <div className="lms-page">{children}</div>;
}
